name: Build Android APK

on:
  push:
    branches: [main, master]
    tags: ['v*']
    paths:
      - 'ewp-core/ewpmobile/**'
      - 'ewp-core/transport/**'
      - 'ewp-core/protocol/**'
      - 'ewp-core/tun/**'
      - 'ewp-core/common/**'
      - 'ewp-core/go.mod'
      - 'ewp-core/go.sum'
      - 'ewp-android/**'
  pull_request:
    paths:
      - 'ewp-core/ewpmobile/**'
      - 'ewp-android/**'
  workflow_dispatch:

jobs:
  build-aar:
    name: Build AAR
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ewp-core

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true
          cache-dependency-path: ewp-core/go.sum

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android NDK
        run: |
          echo "y" | sdkmanager "ndk;25.2.9519653" --sdk_root="$ANDROID_HOME"
          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/25.2.9519653" >> $GITHUB_ENV

      - name: Install gomobile
        run: |
          go install golang.org/x/mobile/cmd/gomobile@latest
          go install golang.org/x/mobile/cmd/gobind@latest

      - name: Initialize gomobile
        run: gomobile init

      - name: Add golang.org/x/mobile dependency
        run: go get golang.org/x/mobile@latest

      - name: Download Go modules
        run: go mod download

      - name: Build Android AAR
        env:
          GOFLAGS: -mod=mod
        run: |
          mkdir -p output/android
          gomobile bind \
            -v \
            -target=android \
            -androidapi=21 \
            -ldflags="-s -w" \
            -o output/android/ewp-core.aar \
            ./ewpmobile

      - name: Upload AAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: ewp-core-aar
          path: ewp-core/output/android/ewp-core.aar
          retention-days: 1

  build-apk:
    name: Build APK
    runs-on: ubuntu-latest
    needs: build-aar

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Download AAR artifact
        uses: actions/download-artifact@v4
        with:
          name: ewp-core-aar
          path: ewp-android/app/libs/

      - name: Inspect AAR contents
        run: |
          echo "=== libs dir ==="
          ls -la ewp-android/app/libs/
          echo "=== all AAR entries ==="
          unzip -l ewp-android/app/libs/ewp-core.aar
          echo "=== extract and inspect classes.jar ==="
          unzip -o ewp-android/app/libs/ewp-core.aar classes.jar -d /tmp/aar-inspect/
          jar tf /tmp/aar-inspect/classes.jar | head -50

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.5'

      - name: Build APK
        working-directory: ewp-android
        env:
          SIGNING_KEYSTORE_B64: ${{ secrets.SIGNING_KEYSTORE_BASE64 }}
          SIGNING_STORE_PASSWORD: ${{ secrets.SIGNING_STORE_PASSWORD }}
          SIGNING_KEY_ALIAS: ${{ secrets.SIGNING_KEY_ALIAS }}
          SIGNING_KEY_PASSWORD: ${{ secrets.SIGNING_KEY_PASSWORD }}
        run: |
          if [ -n "$SIGNING_KEYSTORE_B64" ]; then
            echo "Building signed Release APK"
            echo "$SIGNING_KEYSTORE_B64" | base64 -d > /tmp/ewp-release.jks
            export SIGNING_KEYSTORE_PATH=/tmp/ewp-release.jks
            gradle assembleRelease
          else
            echo "No keystore configured, building Debug APK"
            gradle assembleDebug
          fi

      - name: Collect APKs
        run: |
          mkdir -p apk-output
          find ewp-android/app/build/outputs/apk -name "*.apk" \
            ! -name "*unsigned*" \
            -exec cp {} apk-output/ \;
          ls -lh apk-output/

      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ewp-android-apk
          path: apk-output/*.apk
          retention-days: 30

      - name: Upload APKs to Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: apk-output/*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
